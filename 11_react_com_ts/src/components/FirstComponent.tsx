import * as React from 'react';

export interface IAppProps {
}

export default function FirstComponent (props: IAppProps) {
  return (
    <div>
      <h1>Meu primeiro componente</h1>
    </div>
  );
}
